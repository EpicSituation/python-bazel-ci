module(name = "python_bazel_ci")

# Load rules_python
bazel_dep(name = "rules_python", version = "0.25.0")

# Use pip_parse instead of pip.install()
load("@rules_python//python:repositories.bzl", "pip_parse")

pip_parse(
    name = "python_deps",
    requirements_lock = "@//:requirements.txt",
)
